<%= form_for(@comment) do |f| %>
    <%= hidden_field_tag(:post_id, @comment.post_id) %>
    <%= f.label :content %><br>
    <%= f.text_area :content, size: "60x12", placeholder: "Type the comment here" %><br>

    <%= f.submit %>
<% end %>